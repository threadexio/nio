#pragma once

#include "nio/base/stream.hpp"

namespace nio {
	namespace ip {

		/**
		 * @brief A type representing all IP streams.
		 *
		 */
		using stream = base::stream;

	} // namespace ip
} // namespace nio